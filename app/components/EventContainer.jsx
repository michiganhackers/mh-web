import React from 'react';
import styled from 'styled-components';
import Rectangle from 'react-rectangle';


const EventContainer = ({headerText, subText}) => (
	<Box>
		<Header> this.headerText
			<subHead>this.subText
			</subHead>
		</Header>
	</Box>

);

render() {
	return (
		
	);
}